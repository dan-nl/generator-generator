'use strict';

/**
 * @type {*[]}
 */
var prompts = [
  {
    default: true,
    message: 'create a generator project',
    name: 'create-generator',
    type: 'confirm'
  }
];

module.exports = prompts;
